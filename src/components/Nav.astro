---
const links = [
  { label: 'Services', href: '#services', hasDropdown: true },
  { label: 'Work', href: '#work' },
  { label: 'How it works', href: '#how-it-works' },
  { label: 'Pricing', href: '/pricing' },
  { label: 'Blog', href: '/blog' },
];
---

<header class="nav">
  <div class="nav__inner">
    <a href="/" class="nav__logo" aria-label="Moonb homepage">
      <img src="/assets/logos/moonbee-icon.svg" alt="" class="nav__logo-icon" width="28" height="28" />
      <span class="nav__wordmark">moonb</span>
    </a>

    <nav class="nav__links" aria-label="Main navigation">
      {links.map((link) => (
        <a href={link.href} class="nav__link">
          {link.label}
          {link.hasDropdown && (
            <svg class="nav__arrow" width="10" height="6" viewBox="0 0 10 6" fill="none" aria-hidden="true">
              <path d="M1 1L5 5L9 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          )}
        </a>
      ))}
    </nav>

    <a href="#" class="nav__cta" data-cal-link="team/moonb/book-a-demo" data-cal-namespace="book-a-demo" data-cal-config='{"layout":"month_view"}'>
      Book a Call
    </a>

    <button class="nav__toggle" aria-label="Open menu" aria-expanded="false" id="nav-toggle">
      <span></span><span></span>
    </button>
  </div>
</header>

<div class="nav-mobile" id="mobile-menu" aria-hidden="true">
  {links.map((link) => (
    <a href={link.href} class="nav-mobile__link">{link.label}</a>
  ))}
  <a href="#" class="nav-mobile__cta" data-cal-link="team/moonb/book-a-demo" data-cal-namespace="book-a-demo" data-cal-config='{"layout":"month_view"}'>
    Book a Call
  </a>
</div>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: var(--z-nav);
    background: var(--white);
    transition: background 300ms ease;
  }

  .nav.is-scrolled {
    background: var(--white);
    box-shadow: 0 1px 0 rgba(0,0,0,0.04);
  }

  .nav__inner {
    display: flex;
    align-items: center;
    max-width: calc(var(--max-w) + 6rem);
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
  }

  .nav__logo {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-shrink: 0;
    margin-right: 2rem;
  }

  .nav__logo-icon { width: 26px; height: 26px; }

  .nav__wordmark {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 19px;
    color: var(--heading);
    letter-spacing: -0.04em;
  }

  .nav__links {
    display: flex;
    align-items: center;
    gap: 0;
  }

  .nav__link {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-family: var(--font-heading);
    font-size: 15px;
    font-weight: 500;
    color: var(--heading);
    padding: 8px 14px;
    transition: color 140ms ease;
    white-space: nowrap;
  }

  .nav__link:hover { color: var(--body-muted); }

  .nav__arrow {
    width: 10px;
    height: 6px;
    opacity: 0.5;
    transition: transform 200ms ease;
  }

  .nav__link:hover .nav__arrow { transform: translateY(1px); }

  /* CTA — thinner, bolder, pill */
  .nav__cta {
    margin-left: auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 6px 16px;
    background: var(--btn-primary-bg);
    color: var(--btn-primary-text);
    font-family: var(--font-heading);
    font-size: 14px;
    font-weight: 700;
    border-radius: var(--r-pill);
    transition: background 200ms ease;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .nav__cta:hover { background: #333; }

  .nav__toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    padding: 8px;
    margin-left: auto;
    background: none;
    border: none;
    cursor: pointer;
  }

  .nav__toggle span {
    display: block;
    width: 20px;
    height: 1.5px;
    background: var(--heading);
    border-radius: 1px;
  }

  .nav-mobile {
    display: none;
    position: fixed;
    top: 64px;
    left: 0;
    right: 0;
    z-index: calc(var(--z-nav) - 1);
    background: var(--white);
    padding: 8px 24px 24px;
    flex-direction: column;
    gap: 2px;
  }

  .nav-mobile.is-open { display: flex; }

  .nav-mobile__link {
    display: block;
    padding: 14px 12px;
    font-family: var(--font-heading);
    font-size: 16px;
    font-weight: 500;
    color: var(--heading);
    border-radius: var(--r-md);
  }

  .nav-mobile__link:hover { background: var(--beige); }

  .nav-mobile__cta {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 12px;
    padding: 12px 28px;
    background: var(--btn-primary-bg);
    color: var(--btn-primary-text);
    font-family: var(--font-heading);
    font-size: 15px;
    font-weight: 700;
    border-radius: var(--r-pill);
  }

  @media (max-width: 860px) {
    .nav__links { display: none; }
    .nav__cta { display: none; }
    .nav__toggle { display: flex; }
  }
</style>

<script>
  // Nav scroll — grey background on scroll
  const nav = document.querySelector('.nav');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 20) {
      nav?.classList.add('is-scrolled');
    } else {
      nav?.classList.remove('is-scrolled');
    }
  }, { passive: true });

  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('mobile-menu');

  toggle?.addEventListener('click', () => {
    const isOpen = menu?.classList.toggle('is-open');
    toggle.setAttribute('aria-expanded', String(isOpen));
    menu?.setAttribute('aria-hidden', String(!isOpen));
  });

  menu?.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => {
      menu.classList.remove('is-open');
      toggle?.setAttribute('aria-expanded', 'false');
      menu.setAttribute('aria-hidden', 'true');
    });
  });
</script>
