---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Your Creative Team | Moonb">

  <section class="hero">
    <div class="hero__inner">
      <h1 class="hero__title" data-reveal>
        Your own creative team.
      </h1>

      <p class="hero__sub" data-reveal data-reveal-delay="1">
        Moonb gives you a dedicated team of designers, editors, and motion artists that work as part of your brand. Great work, on repeat.
      </p>

      <div class="hero__actions" data-reveal data-reveal-delay="2">
        <a href="#" class="hero-btn-primary" data-cal-link="team/moonb/book-a-demo" data-cal-namespace="book-a-demo" data-cal-config='{"layout":"month_view"}'>
          Book a Call
        </a>
        <a href="#showreel" class="hero-btn-secondary">
          <svg width="22" height="22" viewBox="0 0 22 22" fill="none" aria-hidden="true" class="hero-play-icon">
            <path d="M5.5 3C5.5 2.17 6.46 1.68 7.13 2.17L17.5 10.17C18.05 10.57 18.05 11.43 17.5 11.83L7.13 19.83C6.46 20.32 5.5 19.83 5.5 19V3Z" fill="currentColor"/>
          </svg>
          Watch the Video
        </a>
      </div>
    </div>

  </section>

</BaseLayout>

<style>
  .hero {
    padding-top: 100px;
    padding-bottom: 0;
    background-color: var(--white);
    overflow: hidden;
  }

  .hero__inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .hero__title {
    max-width: 700px;
    font-family: var(--font-heading);
    font-size: 52px;
    font-weight: 500;
    line-height: 1.06;
    letter-spacing: -0.02em;
    color: var(--heading);
  }

  .hero__sub {
    max-width: 520px;
    margin-top: 18px;
    font-size: 17px;
    line-height: 27px;
    letter-spacing: -0.22px;
    color: var(--body);
  }

  .hero__actions {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 28px;
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 88px;
    }

    .hero__title {
      font-size: 40px;
      letter-spacing: -1.2px;
      line-height: 44px;
    }

    .hero__sub {
      font-size: 16px;
      line-height: 24px;
    }

    .hero__actions {
      flex-direction: column;
      width: 100%;
    }

    .hero-btn-primary,
    .hero-btn-secondary {
      width: 100%;
      justify-content: center;
    }

    .hero__devices {
      gap: 12px;
      margin-top: 40px;
    }

    .device--phone { width: 90px; }
    .device--laptop { width: 200px; }
    .device--tablet { width: 100px; }
  }

  @media (max-width: 480px) {
    .hero__title {
      font-size: 34px;
      line-height: 38px;
    }

    .device--phone { width: 70px; }
    .device--laptop { width: 160px; }
    .device--tablet { width: 80px; }
  }
</style>
